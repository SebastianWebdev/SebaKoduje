import React from "react"
import styles from "../styles/footer.module.css"

const Footer = ({ className }) => <footer className={`${className} ${styles.container}`}>footer</footer>

export default Footer
